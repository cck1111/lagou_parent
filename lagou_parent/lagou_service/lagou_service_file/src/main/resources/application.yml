spring:
  servlet:
    multipart:
      max-file-size: 10MB  #单个文件大小的限制
      max-request-size: 10MB #设置上传文件的总大小
server:
  port: 9008
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:6868/eureka
  instance:
    prefer-ip-address: true
feign:
  hystrix:
    enabled: true
